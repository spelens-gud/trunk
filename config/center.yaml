# Center 服务配置

# 日志配置
logger:
  service_name: "center"
  environment: "dev"
  log_level: "info"
  console: true
  file: true
  file_path: "./logs/center"
  file_name: "center.log"
  max_size: 100
  max_age: 30
  max_backups: 10
  compress: true
  enable_caller: true
  enable_stacktrace: true
